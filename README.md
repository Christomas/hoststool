# hoststool 1.9.8
适用于linux发行版（ubuntu debian centos 红帽）的Hosts工具，可以有效解决无法访问国外站点的问题，同步Android源代码必备工具，自带最新hosts数据库

使用方法：

1使用前先安装依赖包
sudo apt-get install -y python-qt4 pyqt4-dev-tools qt4-designer

2然后执行：
sudo python hoststool.py

即可！
